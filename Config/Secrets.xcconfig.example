// Copy to Secrets.xcconfig (gitignored) and set the operations-owned token
// for TestFlight / App Store builds. Do not commit the real token.
//
// Then add this line to Release.xcconfig:
// #include "Secrets.xcconfig"
PAYMENT_API_TOKEN =
