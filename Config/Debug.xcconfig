#include "Shared.xcconfig"

DEBUG_INFORMATION_FORMAT = dwarf
SWIFT_OPTIMIZATION_LEVEL = -Onone
SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG
GCC_PREPROCESSOR_DEFINITIONS = DEBUG=1
ONLY_ACTIVE_ARCH = YES
ENABLE_TESTABILITY = YES
MTL_ENABLE_DEBUG_INFO = INCLUDE_SOURCE

// Match production routing for device acceptance tests. A local endpoint can
// still be supplied from Secrets.xcconfig when backend development needs it.
CONTENT_CONFIG_ENDPOINT = https:/$()/safthwysdk.antieh.com/api/v1/content-config
STOREKIT_ENABLED = YES

// Inject only the operations-provided CLIENT_API_TOKEN into a local Debug build.
// The spreadsheet's generic TOKEN belongs to the Facebook parameter block and is not valid here.
PAYMENT_API_TOKEN = $(PBM_CLIENT_API_TOKEN)

// Optional, ignored local overrides keep personal signing out of the operations package.
#include? "Secrets.xcconfig"

// Local device build uses the already-installed team development profile.
DEVELOPMENT_TEAM = ADR4GMT9V3
CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = Apple Development
PROVISIONING_PROFILE_SPECIFIER = Primal_Legends_Development_YU_KANG_2026
