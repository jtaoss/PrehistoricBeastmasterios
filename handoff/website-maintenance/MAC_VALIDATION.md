# Mac 交接前验证

日期：2026-09-22（UTC+8）。这里只记录实际执行结果，不代表 Windows 已实现。

## 本包验证

- `node --test verify-public-config.test.mjs`：4 组通过，涵盖三个样例、缺失字段/错误类型/身份/修订号、URL 限制、只读 GET/响应状态/缓存/正文上限。
- 默认 OFF 样例检查通过：`websiteOnly=false, revision=0, updatedAt=""`。
- 公共正式接口仅执行一次无凭证 GET：2026-09-22 15:39 左右返回 HTTP 401，未通过 HTTP 200 契约检查。没有执行管理写入、没有改变线上维护状态。
- Windows 需核对新接口的路由及鉴权；此状态不足以确认接口是否已经存在，不应通过移除其他接口鉴权来修复。

## 客户端事实

- 官网通过 App 内 WKWebView 加载，维护时不调用 Safari 打开官网。
- 仅用户点击已登记的外站客服链接时使用系统浏览器。
- App 已覆盖安装并独立启动到 iPhone X，版本 1.0.12 (49)，首页正常，已有第 3 波远征入口仍在。
- 后台驱动的真实 ON/OFF、官网/客服及付款期间切换仍待联调。当前设备仍保留项目原有的小游戏内部测试开关，不代表正式提审包已就绪。

## 交接内容安全边界

只复制契约、样例、检查脚本和两个独立客户端参考文件；不复制完整配置、Secrets.xcconfig、支付 token、签名文件、玩家数据、真机日志或旧后台源码。共享文件夹原有交付物保持不变。

## 官网原样展示修订（2026-09-22）

- 用户要求移除全部 App 附加界面：当前官网控制器只有 WKWebView 根视图，没有原生标题、提示、按钮、动画、错误页或弹窗，也不注入网页内容。
- 已允许官网自己的 HTTP 错误页原样显示。网络失败不替换成 App 错误页、不恢复游戏；导航白名单和桥接隔离不变。
- Debug iOS arm64 开发签名构建通过。配置/网络/导航测试 Release 与 Debug 各 45 项、路由竞争 31 项、JS 存档 12 场景、原网页展示静态防回归、付款展示 44 项、ATT 28 场景全部通过。
- 初次完成修订时 USB 设备列表为空，未能安装；随后用户重新连接设备，见以下安装补充。

### 纯官网版本真机重装（2026-09-22 17:27 UTC+8）

- 重新构建 Debug 1.0.12 (49) 成功，8 个原生代码文件签名及 App 资源封印验证通过。
- 17:27:10 对已连接 iPhone X 覆盖安装成功，未卸载或执行数据清理；随后独立启动成功，返回 PID 1491。
- 17:27:32 准备截图时设备已断开，USB 列表为空。因此本次仅确认安装和启动命令成功，没有取得新截图或确认当前官网加载结果；不将其写成官网/开关完整验收通过。
