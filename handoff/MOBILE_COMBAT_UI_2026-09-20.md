# 手机战斗界面精简

## 本次范围

- 竖屏战斗改为全屏战场 + 悬浮生命/守护目标/暂停，移除常驻的多排资源、伙伴卡、路线、存档和设置工具栏。
- 底部「建造」展开卡牌抽屉；可收起，展开时不缩小画布。收起会取消选牌/拖拽，避免遗留凝时状态。
- 近景跟随角色，可切换全景查看完整战场。建造时冻结镜头，绘制与触点共用变换。Boss 血条固定在屏幕内。
- 手机/触屏不显示 E/J/K/Esc 等键盘提示；桌面快捷键保留。营地补了可触摸的「取消搬迁」。
- 暂停菜单保留音量/设置、手动保存、资源/伙伴摘要及返回营地。未修改付费、数值、存档格式或发奖流程。
- 必修新手教学保留原来的独立布局及强引导目标。

## 验证

- Node 测试：19 个测试文件通过（测试运行器报告 21 项），含触屏提示、镜头边界与坐标往返测试。
- 浏览器竖屏：320×568、375×812、390×844、430×932、768×1024；无横向溢出，核心按钮在屏幕内且中心命中自身，触控目标至少 44px。
- 320×568 展开建造栏：四张卡、建造/雇佣页签、切换武器、移动及技能可触达；可正常收起。
- 第 1 关普通目标、第 3 关特殊目标/Boss、事件互动、全景开关、暂停/恢复经过 UI 检查；不占用玩家真实订单/付款。
- 1280×900 桌面恢复完整布局，技能及互动快捷键提示正常。
- 打包后的经典 JS bundle 用 WKWebView.loadFileURL 验证：ready=true，无启动错误。
- Xcode Debug 真机架构编译/签名通过；日志：`/tmp/emberwild-mobile-combat-build.log`。

本次未覆盖安装到手机；以上浏览器与 WebKit 验证不等同于实体 iPhone 实机触摸验收。手机安装后重点确认摇杆 + 技能同时触摸、拖卡落点，以及刘海/底部安全区。

## 后续安装回执

2026-09-20 16:48（UTC+8），用户确认「安装」后已通过 Xcode 覆盖安装到 iPhone X（iOS 16.7.16），未卸载或清空数据。Xcode 显示 Running；新进程 732 的 `PBM-WEB-CHECK` 在启动后 1 秒及 4 秒均为 `gameBootReady=true`、`readyState=complete`。战斗触摸体验仍由用户实机验收。
